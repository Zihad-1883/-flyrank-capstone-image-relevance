/**
 * AppError Custom Error Classes
 * 
 * Custom operational error classes (BadRequestError, NotFoundError, UnauthorizedError, etc.)
 * with HTTP status codes for consistent error handling across all modules.
 */
