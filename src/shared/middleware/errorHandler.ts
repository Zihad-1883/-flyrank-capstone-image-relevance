/**
 * Global Error Handler Middleware
 * 
 * Intercepts all unhandled errors in Express routes, formats structured JSON
 * error responses with appropriate 4xx/5xx status codes, and logs system exceptions.
 */
